package ies.app;

import ies.puero.impl.Animal;
import ies.puero.impl.Fecha;

import java.util.ArrayList;
import java.util.List;

public class AppAnimal {

    static List<Animal> animales;
    static  List<Fecha> fechas;

    public static void main(String[] args) {
        String fecha1 = "22/01/2024";
        Animal gato = new Gato ("mario", 12345678);
        animales = new ArrayList<>();
        fechas = new ArrayList<>();

        fechas

        if (!fecha1.contains(fecha1)) {
            fechas.add(fecha1);
        }
        if (!animales.contains(mario)) {
            animales.add(mario);
        }
        System.out.println(fechas);
        System.out.println(animales);
    }
}
